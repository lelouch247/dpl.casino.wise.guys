class Slots
    def initialize(player)
        puts "----------SLOTS!----------\n"
        #put ascii slot machine here
        puts "\n#{player.name} you have a balance of: #{player.wallet.amount}"
    end
end